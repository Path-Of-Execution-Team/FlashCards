apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: hosted
  labels:
    app.kubernetes.io/part-of: flashcards
spec:
  namespaceSelector:
    matchNames:
      - moomento
  selector:
    matchLabels:
      app: hosted
  podMetricsEndpoints:
    - path: /actuator/prometheus
      targetPort: 8081
      interval: 30s
      scrapeTimeout: 10s
